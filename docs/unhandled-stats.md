# Stat coverage audit — what the model does not (fully) handle

Comprehensive across **every affix source in the game**, not just the two reference builds.
Generated by walking all `.claude/skills/tli-build/cache/SS12.5-*-en.json` bundles (gear/craft/
blend/corrosion, divinity slates, ethereal prisms, tower sequences, pactspirits, legendaries/
uniques, hero memories/revival, hero traits, kismets, miscellaneous), instantiating each affix
template with a concrete number, applying the same normalization the parser does
(`"N %"`→`"N%"`, `(a/b/c/d/e)` tier arrays, `N:` prefixes), and running every line through the
live `classify()` in `buildParser.ts`. A line is a "gap" iff `classify()` returns `null` **and**
the wording is DPS-relevant for a melee cold/fire/erosion + Blast-Nova-bomb hero.

> Reproduce: the audit script lives in the skill flow (walk bundles → `norm()` → `classify()`).
> Off-build families (Spell, Sentry, Terra, Spirit Magus, Trauma/Wilt/Ignite ailments, Barrage,
> Channeled, Spacetime/Purgatory/gunner traits, Drop-Quantity/Reward economy) are filtered out.

**Status legend**

| tag | meaning | cost to close |
|---|---|---|
| **PARSER** | model bucket already exists — only a `PATTERNS` regex is missing | one line + a `classify` test |
| **MODEL** | no bucket anywhere — needs a `mechanics.md` row + `damageModel.ts` change + failing test | small→large |
| **UPTIME** | folded into a calibration constant (`hits_per_bomb`) or full-uptime assumption, by design | leave unless an A/B drifts |
| **OFF-BUILD** | correct no-op for this hero (other archetype / pure defense) | none |

Source tags in brackets show where each line appears: `gear` `slate` `prism` `sequence` `pact`
`unique` `memory` `trait` `kismet`. Multi-source lines (e.g. `[gear,slate,pact,unique,memory]`)
are the highest-value gaps — they show up everywhere this hero shops.

---

## 1. Cheap PARSER wins — bucket exists, just needs a regex (the "simple things")

These route to a bucket the model **already** has. Closing them is a one-line `PATTERNS` entry
each (+ a `classify` test). Ordered by how many sources carry them.

| stat line | sources | routes to | status |
|---|---|---|---|
| `Adds N–M {Fire\|Lightning\|Erosion} Damage to the gear` | gear, unique, sequence | `base.weapon_flat_*` | **✅ DONE** |
| `+X% Erosion Damage` | gear, slate, pact, unique, memory | `increased.erosion` | **✅ DONE** |
| `+X% Fire Damage` / `+X% Lightning Damage` | gear, slate, pact, unique, memory | `increased.fire` / `increased.lightning` | **✅ DONE** |
| `+X% chance to deal Double Damage` | gear, kismet, unique, trait | `crit.double_damage_chance_pct` | **✅ DONE** (see §2 — was a MODEL gap) |
| `+X% Attack Critical Strike Damage` | unique, memory | `crit.damage_pct` (made `Attack ` optional in the regex) | **✅ DONE** |
| `+X% Deterioration Damage / Chance` · `+X% additional Deterioration Duration` | gear, slate, unique, kismet | `deterioration.*` (lazily created, additive) | **✅ DONE** — inert until an Obliterate base supplies `hit_damage_pct` (mechanics.md#deterioration). Bing export 67%→74% |
| `+X% Demolisher Charge Restoration Speed` · `Projectile Quantity +N` · `+X% additional Bomb Damage` | gear, slate, unique, kismet, trait | `rotation.demolisher_resto_pct` / `rotation.proj_added` / `additional.bomb` | **✅ DONE** |

Note: several lines that *look* like gaps already parse once normalized — `+X% Mark effect`,
`+X% Frostbite Effect`, `+X% Warcry Effect`, `+X% Aura Effect`, `+X% additional Attack Damage`
are all handled; they only surfaced in raw scans because slate text uses `"N %"`.

## 2. MODEL gaps — no bucket yet (need a mechanics.md row first)

### Universal multipliers (hit both builds)
| stat line | sources | note |
|---|---|---|
| `+X% chance to deal Double Damage` | gear, kismet, unique, trait | **✅ DONE** — `crit.double_damage_chance_pct`, whole-hit ×(1 + min(chance,100)/100), crit-independent (mechanics.md#double-damage) |
| `For every 1% overflown Double Damage Chance → 1% chance for Quadruple Damage` | trait | **deferred** — overflow rider; needs measurement + Rage-engine context |
| `+X% chance to Multistrike` / `Multistrikes deal X% increasing damage` | gear, slate, pact, unique, kismet | **deferred** — no-op on Blast Nova bombs (confirmed); unmodeled for Spectral Slash melee (needs measurement) |

### Damage-type structure (the fire/erosion path)
| stat line | sources | note |
|---|---|---|
| `+X% Fire Damage` / `+X% Lightning Damage` (increased) | gear, slate, pact, unique, memory | **✅ DONE** — `increased.fire`/`increased.lightning` (mechanics.md#double-damage adjacent row) |
| `+X% Gear Elemental Damage` | sequence, unique | **deferred** — elemental analogue of `gear_phys_pct`; would scale weapon-base elemental, which no build rolls yet (double-speculative) |
| `Converts X% of Physical Damage to {Fire\|Cold\|Erosion\|Lightning}` (and 50% variants, `Converts 99% of Cold to Fire`, `… Lightning to Cold/Fire`) | gear, slate, unique, memory | model has the `conversion` array (Bing hand-encodes it); no regex routes `Converts …` lines. PARSER-ish, but partial-% + chained conversions need the overflow logic exercised |
| `Adds X% of {Physical\|Cold\|Fire\|Lightning\|Elemental} Damage as {Fire\|Lightning\|Erosion}` | gear, unique, trait, kismet | only `gain_phys_as_cold_pct` exists; every other gain-as pairing is unmodeled |
| `Adds N–M {Fire\|Lightning\|Erosion} Damage to Attacks (and Spells)` | gear, unique, sequence | global flat adds (× effectiveness) — parser routes only phys + cold; no fire/lightning flat bucket (erosion flat bucket exists, no regex) |

### Projectile / geometry (bomb single-target)
| stat line | sources | note |
|---|---|---|
| `+X% Projectile Damage` / `+X% additional Projectile Damage` | gear, slate, pact, unique | bombs are projectiles → real single-target; no `increased.projectile` tag |
| `+X% Projectile Speed` | gear, slate, pact, unique, memory | **UPTIME** — folded into `hits_per_bomb` (mechanics.md#bing-skill-area) |
| `X% of Projectile Speed bonus also applied to Projectile Damage` (Gale) | gear, slate, unique | conditional cross-scaling; unmodeled |
| `+X% Projectile Critical Strike Damage / Rating` | slate | projectile-scoped crit sub-type; no bucket |
| `+1 Parabolic Projectile Split` / `+N Beams` / `+N Horizontal Projectile Penetration` / `Projectile Quantity +N` | gear, slate, pact, unique | extra-projectile count; model scales single-target by fixed `(proj_base+proj_added)`, doesn't parse gear that raises it |

### Aura-effect variants
| stat line | sources | note |
|---|---|---|
| `+X% {Cruelty\|Frigid Domain\|Precise Projectiles\|Radical Order\|Erosion Amplification} Aura Effect` | gear, sequence, unique | only generic `+X% Aura Effect` is read; each named-aura scaler is dropped. PARSER if the build runs that aura |
| `+X% Fervor effect` / `+X% additional Fervor Duration` | slate, unique | Fervor effect handled; Fervor **Duration** is not (matters only if uptime < 100%) |

### Conditional additional-damage families (would be UPTIME once assigned)
`+X% additional damage while having Fervor` · `… against Frozen enemies` · `… if you have lost
Life recently` · `… while standing still` (ramping) · `… for every Blessing at max stacks
(multiplies)` · `… when holding a Two-Handed Weapon` · `+X% Attack Damage while Tenacity Blessing
is active` — all **MODEL/UPTIME**: each is an `additional.*` or `increased.*` gated on a state
this build likely satisfies at full uptime, but none is parsed. `[slate, unique, trait, pact]`.

## 3. Rehan Rage / Berserk / Seething engine (trait) — MODEL, mostly full-uptime today

The Fury's Onslaught / Rage Infusion / Growing Anger trait chain drives a whole subsystem the
model only approximates via `manual_overrides.json`:

| stat line | note |
|---|---|
| `+X% additional Seething Spirit Damage` / `Seething Spirit appears and auto-attacks` | a second damage source (auto-attacker), unmodeled |
| `+X% additional damage per 25 Rage` (Rage Infusion, ≤8 stacks) | **UPTIME** — full stacks assumed |
| `+1% chance to deal Double Damage every 5 Rage` / `+X% Critical Strike Rating for every 1 Rage` | Rage feeds Double Damage + crit — both unmodeled |
| `Generates X Rage/s while Berserk` / `each Melee Skill generates 10 Rage` / consumption lines | Rage economy — assumed sustained, not simulated |
| `Bombs have X% chance to become duds that deal X% additional damage` (Bing trait) | bomb-variance rider, unmodeled |

## 4. Correctly OFF-BUILD (present in the pool, no action)

Filtered but noted so the omission is intentional: Spell/Cast (`Spell Damage`, `Spell Crit`,
`Cast Speed`, `Adds … to Spells`, `Spell Burst`), other summon archetypes (`Sentry`, `Terra`,
`Shadow`, `Spirit Magus`), other ailments as a source (`Ignite`, `Trauma`, `Wilt`, `Affliction`,
`Tangle`, `Numbed`), other-hero traits (`Spacetime`, `Purgatory`, `Burst`, `Barrage`, `Ammo`,
`Channeled`, `Vendetta`, `Mercury`), positional/economy (`Distant`/`Nearby` enemies, `Drop
Quantity`, `Reward`, `Compass`, `Flame Fuel`), and pure defense/QoL (ES, Max Life, Resistances,
Reflection, `damage taken`, `applied to Life`, `from Mana before Life`, Movement, immunities).

---

## How to close a gap (invariants — do not skip)

1. **PARSER wins** (§1): add the `PATTERNS` row routing to the existing bucket + a `classify`
   test. The mechanic is already cited, so no new `mechanics.md` row — but hand-encoded
   snapshots (Bing) stay authoritative regardless.
2. **MODEL gaps** (§2–3): `mechanics.md` row with a confidence flag **first**, then a failing
   test (`damageModel.test.ts` primitive or `bing1/blast-nova.test.ts` build step), then the
   bucket. Invariants #1 and #4.
3. **UPTIME** items are deliberate folds, not bugs — split one out only when a measured A/B shows
   the constant drifts with that stat.
